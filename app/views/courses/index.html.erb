<h1>Course List</h1>


<ul class="list">
<% @courses.each do |course|%>
    <li>Course Name: <%=course.course_name%>  <%= link_to "Edit Course", edit_course_path(course) %> <%= link_to "Destroy Course", course_path(course), method: :delete %></li>
        <p>Hours: <%=course.hours%>hours/week</p>
        
        <br>
    
<%end%>
</ul>