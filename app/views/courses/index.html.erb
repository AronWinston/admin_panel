<h1>Course List</h1>

<a href="/courses/new">New Course</a>

<ul class="list">
<% @courses.each do |course|%>
    <p>Course: <%=link_to "#{course.course_name}", course_path(course)%>  <%= link_to "Edit Course", edit_course_path(course) %> <%= link_to "Destroy Course", course_path(course), method: :delete %></li>
        <p>Hours: <%=course.hours%>hours/week</p>
        
        <br>
    
<%end%>
</ul>