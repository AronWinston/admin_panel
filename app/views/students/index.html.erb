

<ul class="list">
<% @students.each do |student|%>
    <p>student: <%=student.firstname%> <%=student.lastname%>  <%= link_to "Edit student", edit_student_path(student) %> <%= link_to "Destroy student", student_path(student), method: :delete %></p>
        <li>Age: <%=student.age%></li>
        <li>Education: <%=student.education%></li>
        <br>
    
<%end%>
</ul>
